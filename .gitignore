# Personal config data lives outside this repo now (default:
# ~/.config/gh-copilot-config/profiles). Never track profiles here so the
# tool repo stays code-only and safely shareable.
/profiles/
# compiled binary (built by script/build.sh) + release artifacts
gh-copilot-config
/dist/
# noise
.DS_Store
*.local

# built binary
/gh-copilot-config
